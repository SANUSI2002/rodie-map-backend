# PostgreSQL connection string (Render, Supabase, Railway, or local)
DATABASE_URL="postgresql://user:password@localhost:5432/roadimap?schema=public"

# JWT
JWT_SECRET="change-this-to-a-long-random-string"
JWT_EXPIRES_IN="7d"

# App
PORT=4000
CORS_ORIGIN="http://localhost:3000"
